#!/usr/bin/env bash

set -euo pipefail

if [[ "${RUBY_VERSION}" =~ ^2 ]]; then
  gem update --system=3.4.22
else
  gem update --system
fi

gem install bundler

gems=(
  benchmark-ips
  gem-ctags
  hookup
  foreman
)

for gem in "${gems[@]}"; do
  gem install "${gem}"
done
