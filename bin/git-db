#!/usr/bin/env bash

set -euo pipefail

branch=${1};

[[ -n "${branch}" ]] || exit 1

if git config "branch.${branch}.remote" >/dev/null; then
  git push origin --delete "${branch}"
fi

git branch -D "${branch}"
